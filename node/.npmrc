# npm configuration
# https://docs.npmjs.com/cli/v10/configuring-npm/npmrc

# Save exact versions (no ^ or ~ prefix)
save-exact=true

# Warn if package.json has unsatisfied peerDependencies
engine-strict=true
